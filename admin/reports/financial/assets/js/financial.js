/**
 * Financial Reports JS
 */
(function() {
    'use strict';

    // Print is handled by the browser's native print dialog.
    // CSS @page { size: auto; } is used so Chrome/Edge show the
    // Layout / Orientation option under More settings.
})();
